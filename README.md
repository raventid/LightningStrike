# LightningStrike

An experimental High-Frequency Trading (HFT) orderbook implemented in ATS.

Build the executable:
``` sh
make build
```

Clean the build:
``` sh
make clean
```

Run an executable:
```sh
make run
```
